pcm.!default {
        type plug
        slave.pcm "bs2b"
}

ctl.!default {
        type hw
        card 2
}

pcm.bs2b {
         type ladspa
         slave.pcm "plughw:2,0"
         path "/usr/lib/ladspa/"
         plugins [{
                 label bs2b
                 input {
                       controls [ 725 4.5 ]
                       }
                 }]
}
